# UI8Kit Hugo Site

Build: `hugo --minify`

Dev: `hugo server -D`

Publish dir: `public`

## Structure

- `content/` – Markdown content
- `layouts/` – Templates
- `assets/css/` – Styles (processed by Hugo Pipes)
- `themes/ui8kit/` – Minimal theme glue
